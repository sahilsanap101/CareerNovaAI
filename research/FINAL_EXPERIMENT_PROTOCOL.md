# FINAL EXPERIMENT PROTOCOL

All evaluation phases testing PathForge mathematical viability must explicitly conform to rigorous constraints averting local data-leakage and stochastic manipulations.

## Validation Protocol Limits
1. **The $A0-A6$ Permutation Rules**: Execution must disable components individually in isolation to quantify explicit structural utility (e.g., stripping Market Weights measuring $Utility$ loss against static chronological matrices). 
2. **Fixed Parameter Seeding**: All TS executions routing variables and data compilation arrays *must* run on fixed `.ts` environmental random-number-generator bindings (Base Seed *12345*).
3. **Reproducibility Guarantee**: Commands targeting `/research/experiments/run_final_evaluation.ts` are 100% frozen. Execution yields the exact JSON outputs logging unmodified metric extractions. Wait states or variance anomalies mapping API LLM latency are universally banned inside the evaluation perimeter.
4. **Counterfactual Mutability Checks**: Tests must randomly assign 10-50% noise bounds across node priorities testing topological boundary crashes. If Prerequisite limits fail ($ViolationCount > 0$), the pipeline natively fails overall validation.
